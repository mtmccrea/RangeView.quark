RangeView : SpreadView {
	/* RangeView doesn't yet have a unique implementation, but will be modeled on
	   SpreadView, which would be considered a special case (and ultimately a subclass)
	   of RangeView. In the meantime, SpreadView will provide that functionality, albeit
	   with method and argument names that aren't necessarily suited to thinking about
	   the control in terms of "range". */
}